<UserControl x:Class="BackgroundClicker.ClickerConfig"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BackgroundClicker"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <StackPanel>
        <CheckBox x:Name="IsActive">Enabled</CheckBox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0">Delay (ms)</Label>
            <xctk:IntegerUpDown Grid.Column="1" IsEnabled="{Binding IsChecked, ElementName=IsActive}" TextAlignment="Left"/>
        </Grid>
    </StackPanel>
</UserControl>
